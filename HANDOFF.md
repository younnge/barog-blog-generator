# 인수인계 요약

> 작성일 2026-08-14 · 상태: **`SPEC.md` §15 1단계 구현 완료 (Render 배포 대기)** · 다음 할 일: §15 2단계 (프론트 정적 화면)

---

## 1. 지금까지 뭘 했나

Claude(Cowork)와 대화로 요구사항을 확정하고 문서 3종 + 기준정보 2종을 만들었다. **코드는 아직 한 줄도 없다.**

| 산출물 | 내용 |
|---|---|
| `SPEC.md` | 요구사항 확정본 (16개 섹션) |
| `CLAUDE.md` | 상시 프로젝트 규칙 — 절대 규칙 7개, 디자인 토큰, 작업 방식 |
| `config/branches.json` | 지점 24개 |
| `config/procedures.json` | 시술 10개 카테고리 / 237개 |
| `HANDOFF.md` | 이 문서 |

---

## 2. 만들려는 것

피부과 네트워크(바로그의원) **인하우스 마케터 5명**이 쓰는 블로그 글 초안 생성 사내 웹 도구.
사용자는 **20~30대 여성 비개발자**. 링크 클릭 → 비밀번호 → 버튼 몇 번 → 완성된 글.

기존에 운영 중인 네이버 블로그 자동화 시스템(FastAPI+Render)이 따로 있다.
**그건 손대지 않고, 리뉴얼된 신규 버전을 별도로 만드는 것이다.**

---

## 3. 확정된 결정 사항

### 구조
- 프론트: **바닐라 HTML/CSS/JS 단일 페이지** → GitHub 저장소 → **GitHub Pages 링크 배포**. 빌드 도구 없음.
- 백엔드: **FastAPI 신규 서비스 → Render**. Anthropic API 키는 서버 환경변수에만.
- 폴백: 서버가 막히면 "개인 API 키 입력 모드"로 전환 가능. 단 **기본은 항상 서버 모드**, 폴백은 숨겨진 보조 경로.

### 접근·저장
- 접근 제한: **공통 비밀번호 1개**, 백엔드에서 검증, 토큰 30일 유지.
- 생성 이력: **팀 전체 공유**. 누가 언제 어떤 지점·시술로 썼는지 목록 조회·검색·재사용.
- 30일 내 같은 지점+시술 조합이 있으면 입력 화면에 중복 안내 배너.

### 생성 흐름 (핵심)
한 번에 다 뽑지 않는다. **사람이 중간에 개입해 고른다.**
```
입력 → 키워드 10~12개 추천 → 3~5개 선택 → 제목 5개 추천 → 1개 선택(수정 가능) → 본문 생성
```
결과 화면에서는 **소제목 블록 단위로 "이 부분만 다시"** 재생성.

### 입력 항목
- 필수(전부 버튼 선택): 지점 / 시술 / 페르소나 / 독자 타겟 / 톤 / 플랫폼 / 분량
- 선택(접힌 영역): 이벤트·가격·CTA / 참고자료 붙여넣기
- 시술은 **카테고리 칩 → 시술 칩 2단계 + 검색창** (237개라 한 번에 못 펼침)

### 페르소나 (글쓴이 시점) 6종
`원장님` `상담실장` `후기형` `정보 큐레이터` `트렌디 마케터` `직접 입력`
독자 타겟은 **별도 축**으로 분리 (`20대 첫 시술` `30대 관리` `민감 피부` `브라이덜` `남성` `40대+`)

> ⚠️ `후기형`은 의료광고 심의에서 **치료경험담**으로 걸릴 위험이 크다.
> 선택 시 경고 배지 + 프롬프트에서 1인칭 치료결과 서술 억제.

### 의료법 대응 (3중 장치)
1. 생성 단계: 프롬프트에 의료광고법 가이드 주입
2. 검사 단계: 금지어 사전 매칭(무료) + LLM 문맥 판정
3. **차단 단계: 🔴위험 항목이 남아 있으면 복사 버튼 비활성화.** 대안 문구 클릭 한 번으로 교체 → 자동 재검사.

### 플랫폼별 출력
네이버(스마트에디터 붙여넣기 최적화 + 해시태그) / 워드프레스·자사 홈페이지(마크다운+HTML) / 인스타·스레드(캡션형) / 티스토리·브런치(마크다운)

### 디자인
**클리닉 미니멀.** 브랜드 원색 `#B09B8E`.
→ 원색은 흰 배경 대비 2.65:1이라 **텍스트/흰글씨 버튼으로 못 쓴다.** 8단계 팔레트로 확장했고 버튼 배경은 `#7A6355`(5.61:1). `CLAUDE.md`에 CSS 변수로 정리되어 있다.

### 모델 (비용 최적화, 목표 건당 300원 이하)
키워드·제목·의료법 2차 검사 = **Haiku** / 본문·문단 재생성 = **Sonnet**

---

## 4. Phase 구분

| Phase | 범위 | 상태 |
|---|---|---|
| **1** | 글 생성기 전체 (인증·위저드·의료법·이력·플랫폼 포맷) | **이번에 구현** |
| 2 | 이미지 생성 — 썸네일 1장 + 본문 삽입 3~5장, Higgsfield 연동 | 후속 |
| 3 | 자동 발행 — 워드프레스만. **네이버는 공식 API 없어 제외 권장**(계정 제재 리스크) | 필요 시에만 |

Phase 1이 안정화되기 전까지 2·3 코드를 섞지 않는다.

---

## 5. 아직 안 정한 것 (임의로 정하지 말고 물어볼 것)

| # | 항목 | 권장안 |
|---|---|---|
| 1 | **이력 저장소** — Render는 디스크 휘발성이라 외부 저장소 필수 | Supabase 무료 티어 (대안: GitHub Gist) — 6단계에서 결정 |
| ~~2~~ | ~~Render 플랜~~ | ✅ 2026-08-14 확정 — **무료 플랜**으로 시작, 필요 시 Starter 승격 |
| ~~3~~ | ~~저장소 공개 여부~~ | ✅ 2026-08-14 확정 — **Public** (`younnge/barog-blog-generator`) |
| 4 | 작성자 구분 (공통 비번이라 자동 식별 불가) | 저장 시 이름 선택 드롭다운 1회 |
| 5 | **시그니처 시술 `바로그 코어 레이저`** — 첨부 목록에 없음 | 어느 카테고리에 넣을지 확인 필요 |
| 6 | `여드름·트러블`의 `헤어컬러피부` | 카테고리와 안 맞음. 오기 여부 확인 필요 |
| 7 | 기존 시스템의 지점별 스타일 학습 데이터 이식 | 있으면 참고자료 프리셋으로 재활용 |

**config 파일:** `personas.json` `audiences.json` `tones.json` 은 1단계에서 `SPEC.md` §5.3~5.5 그대로 작성 완료.
`compliance.json`(금지어 사전)만 남았고, §6.3을 근거로 4단계에서 만든다.

---

## 6. 다음 할 일 — SPEC.md §15 개발 순서

1. 백엔드 스켈레톤 + 인증 + `/api/config` → **Render 배포까지 먼저 뚫기**
2. 프론트 정적 화면 + 기준정보 JSON 렌더링 (API 없이 화면부터 완성)
3. 키워드 → 제목 → 본문 3단 파이프라인 연결
4. 의료법 검사 + 복사 차단
5. 플랫폼별 포맷 변환
6. 이력 저장·조회
7. 디자인 마감 (여백·모션·빈 화면·에러 문구)
8. 마케터 1명 시범 사용 → 피드백 → 전체 오픈

**각 단계가 끝나면 멈추고 확인받는다.**

---

## 7. 완료 판단 기준 (Phase 1)

가장 중요한 것부터:
- [ ] 비개발자가 **도움 없이 3분 이내**에 첫 글을 뽑는다
- [ ] API 키가 브라우저 어디에서도 안 보인다 (개발자 도구로 직접 확인)
- [ ] 🔴위험 표현이 남으면 복사 버튼이 비활성화된다
- [ ] 네이버 스마트에디터에 붙여넣을 때 줄바꿈이 안 깨진다
- [ ] `branches.json`에 1줄 추가하면 코드 수정 없이 버튼이 늘어난다
- [ ] 본문이 지정 분량 ±20% 내로 나온다

전체 목록은 `SPEC.md` §13.
