# CLAUDE.md — 바로그 블로그 글 생성기

> 이 파일은 프로젝트의 상시 규칙이다. 상세 요구사항은 `SPEC.md`에 있다.
> 작업 전 `SPEC.md`를 먼저 읽고, 충돌하면 `SPEC.md`가 우선한다.

---

## 프로젝트 한 줄 정의

피부과 네트워크(바로그의원) 인하우스 마케터 5명이 쓰는, 블로그 글 초안 생성 사내 웹 도구.
**사용자는 20~30대 여성 비개발자다.** 이 전제가 모든 판단의 기준이다.

---

## 절대 규칙 (위반 금지)

1. **API 키를 프론트엔드 코드·번들·네트워크 응답 어디에도 노출하지 않는다.** 키는 Render 환경변수에만 존재한다.
2. **기준정보를 하드코딩하지 않는다.** 지점·시술·페르소나·독자타겟·톤은 전부 `config/*.json`을 읽어 버튼을 자동 렌더링한다. 지점 추가 = JSON 한 줄 추가로 끝나야 한다.
3. **의료법 검사에서 🔴위험 항목이 남아 있으면 `복사하기` 버튼을 활성화하지 않는다.** 우회 경로를 만들지 않는다.
4. **에러 메시지에 기술 용어를 노출하지 않는다.** 상태코드·스택트레이스·영문 예외명 금지. 대신 다음 행동을 한국어로 안내한다.
5. **브랜드 원색 `#B09B8E` 위에 흰 글씨를 올리지 않는다.** 대비 2.65:1로 접근성 미달이다. 버튼 배경은 `#7A6355`를 쓴다.
6. **빌드 도구를 도입하지 않는다.** 프론트는 바닐라 HTML/CSS/JS 단일 페이지. npm 빌드·번들러·프레임워크 없이 GitHub Pages에 그대로 올라가야 한다.
7. **기존 운영 중인 네이버 블로그 자동화 시스템(FastAPI+Render)을 건드리지 않는다.** 이 프로젝트는 별도 저장소·별도 배포의 신규 버전이다.

---

## 기술 스택 (고정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 프론트 | 바닐라 HTML/CSS/JS 단일 페이지 | GitHub Pages 정적 배포 |
| 폰트 | Pretendard (CDN) | |
| 아이콘 | Lucide (CDN) 또는 인라인 SVG | |
| 백엔드 | FastAPI + Uvicorn | Render 신규 서비스 |
| LLM | Anthropic Claude API | 키워드·제목·검사는 Haiku, 본문·재생성은 Sonnet |
| 인증 | 공통 비밀번호 1개, 서버 검증, 토큰 30일 | |
| 이력 저장소 | **미정** (Supabase 권장) | 결정 전까지 인터페이스만 추상화해둘 것 |

---

## 파일 구조

```
/index.html
/assets/styles.css
/assets/app.js
/config/branches.json      ← 지점 24개 (확정)
/config/procedures.json    ← 10개 카테고리 / 237개 시술 (확정)
/config/personas.json      ← 미작성
/config/audiences.json     ← 미작성
/config/tones.json         ← 미작성
/config/compliance.json    ← 미작성 (금지어 사전)
/server/                   ← FastAPI
/SPEC.md
/CLAUDE.md
```

---

## 디자인 토큰 (CSS 변수로 선언할 것)

```css
:root {
  --brand-50:  #FAF7F5;  /* 섹션 배경 */
  --brand-100: #F2EBE6;  /* 선택된 칩 배경 */
  --brand-200: #E3D6CE;  /* 보더·구분선 */
  --brand-300: #CFBCB0;  /* 비활성 */
  --brand-400: #B09B8E;  /* 브랜드 원색 — 선택 보더·포커스링·진행바 */
  --brand-500: #9A8375;  /* 큰 텍스트(18px+) */
  --brand-600: #7A6355;  /* 주요 버튼 배경(흰 글씨)·링크 */
  --brand-700: #5C4A3F;  /* hover/active·제목 강조 */

  --text:      #1F1B18;
  --text-sub:  #6B625C;
  --border:    #E5E0DC;

  --danger:    #C4453D;
  --warn:      #B4761F;
  --success:   #4A7C59;

  --radius:    14px;
  --shadow:    0 1px 3px rgba(0,0,0,0.06);
}
```

- 본문 16px / 행간 1.7, 콘텐츠 최대 폭 960px 중앙 정렬
- 요소 간 최소 16px, 섹션 간 40px
- 애니메이션 150~200ms ease-out
- 클릭 영역 최소 44×44px, 포커스 링 유지

---

## 화면 흐름 (4단계 위저드)

```
[0] 잠금  →  [1] 입력  →  [2] 키워드 선택  →  [3] 제목 선택  →  [4] 결과
                                                                   ↕
                                                              [5] 이력 탭
```

- 각 단계는 되돌아갈 수 있어야 하고, 새로고침·뒤로가기에도 입력이 날아가지 않아야 한다.
- 본문 생성은 20~40초 걸린다. **스트리밍 또는 최소한 단계별 진행 표시**를 반드시 넣는다. 빈 화면은 금지.
- 시술 선택은 **카테고리 칩 → 시술 칩 2단계** + 검색창. 237개를 한 번에 펼치지 않는다.

---

## 카피 규칙

- 버튼은 동사로: `키워드 추천받기` `제목 추천받기` `본문 생성하기` `복사하기`
- 에러는 원인이 아니라 다음 행동을 말한다
  - ✕ `500 Internal Server Error`
  - ○ `잠시 문제가 생겼어요. 다시 눌러볼까요?`
- 대기 중에는 무슨 일이 일어나는지 문장으로 알린다: `글 구조를 잡고 있어요…`
- 빈 화면(이력 없음)에도 안내 문구 + 다음 행동 버튼을 둔다

---

## 작업 방식

- **`SPEC.md` §15 개발 순서를 지킨다.** 단계를 건너뛰거나 합치지 않는다.
- **각 단계가 끝나면 멈추고 사용자에게 확인받는다.** 승인 없이 다음 단계로 넘어가지 않는다.
- 미확정 항목(`SPEC.md` §14)에 부딪히면 임의로 정하지 말고 물어본다.
- 코드 주석과 커밋 메시지는 한국어로 쓴다.
- 사용자는 비개발자 팀에 배포할 사람이다. 터미널 명령이 필요하면 복붙 가능한 형태로 정확히 알려준다.
