# SPEC.md — 바로그 블로그 글 생성기 (사내 인하우스 마케터용)

> 작성일: 2026-08-14
> 상태: 요구사항 확정본 (Phase 1 기준) — 코드 작성 전
> 개발 환경: VSCode + Claude Code

---

## 1. 개요

### 1.1 목적
피부과 네트워크(바로그의원) 인하우스 마케팅팀 인원이, 지점·시술·페르소나만 선택하면
**네이버 블로그를 비롯한 채널에 바로 쓸 수 있는 완성도 높은 글 초안**을 뽑을 수 있는 사내 전용 웹 도구.

### 1.2 핵심 원칙
| 원칙 | 의미 |
|---|---|
| **비개발자 우선** | 20~30대 여성 비개발자가 설명 없이 3분 안에 첫 글을 뽑을 수 있어야 한다. 전문용어·에러코드·터미널 노출 금지. |
| **선택형 입력** | 자유 입력을 최소화하고 버튼(칩) 선택 위주로 구성. 타이핑은 선택 사항. |
| **단계별 확인** | 한 번에 다 뽑지 않는다. 키워드 → 제목 → 본문 순으로 사람이 중간에 개입해 고른다. |
| **글 목적 분리** | 정보성 글과 홍보성 글은 규제 환경이 다르다. 목적을 먼저 고르게 하고, 정보성에서는 위반 소재를 **입력조차 받지 않는다**. (§5.6) |
| **의료법 안전장치** | 의료광고법 위반 표현이 남아 있으면 복사 자체를 막는다. 검사로 메우지 않고, 생성 단계에서 먼저 막는다. |
| **예쁨은 기능이다** | 쓰고 싶어지는 화면이어야 실제로 쓰인다. 클리닉 미니멀 톤 유지. |

### 1.3 기존 시스템과의 관계
- 현재 운영 중인 네이버 블로그 자동화 시스템(FastAPI + Render)은 **그대로 유지**한다.
- 본 프로젝트는 그것의 **리뉴얼 신규 버전**으로, 별도 저장소·별도 배포로 새로 만든다.
- 기존 시스템의 프롬프트 자산·지점별 스타일 학습 로직은 참고·이식 가능하되, 코드 의존은 두지 않는다.

---

## 2. 사용자

| 항목 | 내용 |
|---|---|
| 사용자 | 바로그의원 인하우스 마케팅팀 **6명** (실사용 5명 + 관리자 1명) |
| 특성 | 20~30대 여성, 비개발자, 마케팅 실무자 |
| 사용 빈도 | 1인당 주 3~10건 예상 (팀 전체 월 100~200건 규모) |
| 사용 기기 | 사무실 데스크톱 크롬 (모바일은 조회 정도만 대응) |
| 관리자 | 정남 (IT팀) — 배포·기준정보 관리·비용 관리 |

### 사용 시나리오
> 마케터 A가 링크를 클릭한다 → 비밀번호를 넣는다(첫 1회) → "역삼점 / 바로그 코어 레이저 / 상담실장 시점 / 20대 첫 시술" 버튼을 누른다
> → 추천 키워드 10개 중 3개를 고른다 → 추천 제목 5개 중 1개를 고른다 → 본문이 생성된다
> → 의료법 검사 결과 "주의" 문구 2개를 대안으로 교체한다 → 복사 버튼을 눌러 스마트에디터에 붙여넣는다.

---

## 3. 범위 (Phase)

### Phase 1 — 글 생성기 (필수, 이번 개발 범위)
- 접근 제한(공통 비밀번호)
- 4단계 위저드: 입력 → 키워드 선택 → 제목 선택 → 본문 생성
- 의료법 컴플라이언스 검사 + 미통과 시 복사 차단
- 팀 공유 생성 이력
- 플랫폼별 출력 포맷 (네이버 / 워드프레스 / SNS)

### Phase 2 — 이미지 생성 (후속)
- 썸네일 1장 + 본문 삽입 이미지 3~5장 자동 생성
- 글 구조(소제목)에 맞춰 삽입 위치 자동 배치
- Higgsfield 연동 기준

### Phase 3 — 자동 발행 (필요 시에만)
- 워드프레스 REST API 자동 발행 (기술적으로 안정적)
- 네이버 블로그는 공식 API 부재 → **자동 발행 대상에서 제외 권장**. 계정 제재 리스크.
- 예약 발행·발행 캘린더

> Phase 1이 안정화되기 전까지 Phase 2·3 코드를 섞지 않는다.

---

## 4. 화면 흐름

```
[0] 잠금 화면 ──── 공통 비밀번호 입력 (1회, 기억됨)
        │
[1] 새 글 만들기 ── ★글 목적(정보성/홍보성) 먼저
        │          지점 · 시술 · 페르소나 · 독자타겟 · 톤 · 플랫폼 · 분량
        │          (+ 접기: 참고자료 붙여넣기 / 이벤트·CTA는 홍보성일 때만)
        ▼  "키워드 추천받기"
[2] 키워드 선택 ─── 추천 키워드 10~12개 카드, 3~5개 선택 (직접 추가 가능)
        ▼  "제목 추천받기"
[3] 제목 선택 ───── 추천 제목 5개, 1개 선택 (직접 수정 가능)
        ▼  "본문 생성하기"
[4] 결과 ────────── 본문 + 의료법 검사 결과 + 문단별 재생성 + 복사/다운로드
        │
[5] 이력 ────────── 팀 전체가 만든 글 목록 (별도 탭)
```

### 화면별 요구사항

**[0] 잠금 화면**
- 로고 + 비밀번호 입력 한 칸. "사내 전용 도구입니다" 한 줄.
- 검증은 반드시 **백엔드에서** 수행 (프론트 비교 금지).
- 성공 시 토큰을 `localStorage`에 저장, 30일 유지.

**[1] 새 글 만들기**
- **맨 위에 `글 목적` 칩 2개**(정보성 / 홍보성). 이 선택이 아래 항목 구성을 바꾼다 (§5.6).
  - 정보성 → `이벤트·가격·CTA` 입력란이 사라지고, 페르소나가 5종으로 줄어든다.
  - 목적을 바꾸면 화면이 즉시 바뀌고, 이미 고른 `후기형` 페르소나는 자동 해제된다.
  - 각 칩 아래에 무엇에 쓰는지 한 줄 설명을 둔다. 비개발자가 용어만 보고 고를 수 없다.
- 기본 입력은 **모두 버튼(칩) 선택**. 스크롤 1~2회 안에 끝나야 함.
- 선택 항목은 화면 상단에 요약 칩으로 계속 표시(무엇을 골랐는지 잊지 않게).
- 고급 옵션(참고자료, 홍보성일 때 이벤트/CTA)은 접힌 상태가 기본.
- "직전에 만든 설정 불러오기" 버튼 1개 (반복 작업 단축).

**[2] 키워드 선택**
- 카드 형태로 10~12개 제시. 각 카드에 `키워드` + `검색의도 한 줄`(정보형/비교형/후기형/예약형) 표기.
- 3~5개 선택 강제(0개면 다음 버튼 비활성).
- 하단에 "직접 추가" 입력 한 칸.

**[3] 제목 선택**
- 5개 제시. 각 제목 아래에 글자수 + 어떤 후킹 방식인지 한 줄 설명.
- 선택 후 **인라인 수정 가능**(제목은 마케터가 손대고 싶어함).
- "다시 추천받기" 버튼.

**[4] 결과**
- 좌: 본문(소제목 구조 그대로 렌더링). 우 또는 상단: 의료법 검사 패널.
- 문단(소제목 블록)마다 마우스 오버 시 **`이 부분만 다시` 버튼** 노출.
- 하단 고정 바: `복사하기` / `.txt 다운로드` / `이력에 저장` / `새로 만들기`
- 플랫폼 선택에 따라 복사 결과 포맷이 달라짐(§7).

**[5] 이력**
- 표: 날짜 / 작성자 / 지점 / 시술 / 제목 / 플랫폼 / 상태
- 행 클릭 시 원문 보기 + 복사. 검색(지점·시술·키워드) 필터.
- 30일 내 동일 지점+시술 조합이 있으면 [1] 단계에서 **"이미 3건 있어요" 안내 배너** 노출(중복 콘텐츠 방지).

---

## 5. 입력 항목 정의

### 5.1 필수 (버튼 선택)

| 항목 | 형태 | 값 출처 |
|---|---|---|
| **글 목적** | 단일 선택 칩 (2개) | 정보성 / 홍보성 — **§5.6**. 다른 항목보다 위에 놓는다. |
| 지점 | 단일 선택 칩 | `config/branches.json` |
| 시술 | 단일 선택 칩 (카테고리 → 시술 2단계) | `config/procedures.json` |
| 페르소나(글쓴이 시점) | 단일 선택 칩 | `config/personas.json` |
| 독자 타겟 | 단일 선택 칩 | `config/audiences.json` |
| 톤 | 단일 선택 칩 | `config/tones.json` |
| 플랫폼 | 단일 선택 칩 | 네이버 / 워드프레스·자사 홈페이지 / SNS / 기타 |
| 분량 | 단일 선택 칩 | 짧게(≈1,200자) / 보통(≈2,000자) / 길게(≈3,000자) |

### 5.2 선택 (접힌 영역)

| 항목 | 형태 | 설명 |
|---|---|---|
| 타겟 키워드 | [2]단계에서 선택 | 검색의도 라벨 포함 |
| 이벤트·가격·CTA | 텍스트 3줄 | **홍보성 모드에서만 표시된다.** 글 말미 자동 삽입. 비우면 일반 예약 유도 문구.<br>정보성 모드에서는 입력란이 사라지고, 값이 전달돼도 서버가 무시한다. |
| 참고자료 | 텍스트 붙여넣기 (최대 8,000자) | 잘 된 기존 글 / 시술 설명자료. 톤과 팩트를 반영. |

### 5.3 페르소나 정의 (제안)

`config/personas.json` 초기값. 각 페르소나는 **시점 + 말투 + 강조점 + 금지사항** 4요소를 갖는다.

| ID | 이름 | 시점·말투 | 강조점 | 주의 |
|---|---|---|---|---|
| `doctor` | 원장님 시점 | 1인칭 전문가, 차분한 존댓말 | 시술 원리, 적응증, 안전성, 사후관리 | 효과 단정 금지, 진단적 표현 주의 |
| `manager` | 상담실장 시점 | 친근한 안내자, 다정한 존댓말 | 상담 과정, 비용·소요시간, 예약 안내 | 가격 표기 시 조건 명시 |
| `review` | 후기형(소비자 시점) | 경험담, 구어체에 가까운 존댓말 | 시술 전 고민 → 과정 → 느낌 | **홍보성 모드 전용.** 환자 체험담은 의료법 위반 소지 高 — 선택 시 경고 배지 필수 |
| `curator` | 정보 큐레이터 | 중립적 설명체, 비교·정리형 | 시술 비교표, 선택 기준, FAQ | 타 병원 비방 금지 |
| `trendy` | 트렌디 마케터 | 짧은 호흡, 후킹형 | 시즌 이슈, 트렌드 연결, SNS 감성 | 과장 표현 필터 강하게 |
| `custom` | 직접 입력 | 사용자가 3줄로 정의 | — | 동일 필터 적용 |

> `review` 페르소나는 국내 의료광고 심의에서 **치료경험담**으로 해석될 위험이 크다.
> **정보성 모드에서는 목록에 아예 나타나지 않는다**(5종만 표시).
> 홍보성 모드에서 선택하면 화면에 "환자 체험담으로 읽힐 수 있어요. 정보 전달 중심으로 자동 조정됩니다" 안내를 띄우고,
> 프롬프트에서 1인칭 치료 결과 서술을 억제한다.

### 5.4 독자 타겟 (제안)
`20대 첫 시술 입문` / `30대 관리·재방문` / `민감·트러블 피부 고민` / `브라이덜·이벤트 대비` / `남성 고객` / `40대+ 안티에이징`

### 5.5 톤 (제안)
`다정한` / `전문적인` / `담백한` / `발랄한`

### 5.6 글 목적 (모드) — 2026-08-19 확정

의료광고 규제가 실제로 잡는 항목은 대체로 정해져 있다. 아래 **금지 5요소**다.

| # | 요소 | 예 |
|---|---|---|
| 1 | 효과 보장·최상급·비교우위 | `100%`, `최고`, `국내 유일`, `~보다 우수한` |
| 2 | 가격·이벤트·할인 | 비급여 가격, 선착순, 할인율 |
| 3 | 전후사진 | `시술 전/후 비교` |
| 4 | 체험담·후기 | 환자 1인칭 치료 결과 서술 |
| 5 | 예약 유도 | 글 말미 예약·상담 유도 문구 |

이 5가지가 없는 글은 대체로 **광고로 보기 어렵다**. 시술 원리 해설·관리법·질병 정보는 광고 규제의 사정권 밖에 가깝다.
반대로 국내 네이버 블로그 실무에서는 2·5번이 필요하다. 그래서 **한 도구에서 목적을 나눈다.**

| | **정보성** | **홍보성** |
|---|---|---|
| 쓰는 곳 | 자사 홈페이지, 해외 사이트, AI 검색 인용 | 국내 네이버 블로그, SNS 이벤트 |
| 목표 | 검색·AI가 인용하는 정보 자산 축적 | 이벤트 고지, 예약 전환 |
| `이벤트·가격·CTA` 입력란 | **숨김** | 표시 |
| 페르소나 | 5종 (`review` 제외) | 6종 전체 |
| 말미 예약 유도 문구 자동 삽입 | **없음** | 있음 |
| 금지 5요소 | **1~5 전부 생성 차단** | 1·3·4 차단, 2·5는 검사 대상 |

**차단은 서버에서 강제한다.**
프론트에서 입력란을 숨기는 것은 편의일 뿐이고, 서버는 `mode`를 받아 정보성이면 `cta` 값이 전달돼도 **무시**하고,
프롬프트에 금지 5요소 전면 차단 규칙을 주입한다. 우회 경로를 만들지 않는다 (CLAUDE.md 절대 규칙 3).

> **주의:** 모드 구분은 규제 준수를 보장하지 않는다. 위반 가능성을 구조적으로 낮추는 장치일 뿐이며,
> 실제 적합성 판단은 의료광고 심의기관 질의 또는 의료법 전문 법무 검토로 별도 확보해야 한다.
> 최종 책임은 게시자(의료기관 개설자)에게 있다.

---

## 6. 의료법 컴플라이언스 (핵심 기능)

### 6.1 동작 방식 — 3중 장치
1. **생성 단계 (사전 통제)**: 선택된 **모드(§5.6)**에 따라 프롬프트가 달라진다.
   - 정보성: 금지 5요소를 **전면 차단**하는 규칙 주입 + `cta` 입력 무시
   - 홍보성: 의료광고법 가이드 주입 + 1·3·4번 요소 차단
   - 위반 소재를 **애초에 만들지 않는 것**이 1차 방어선이다. 검사로 메우려 하면 뚫린다.
2. **검사 단계 (사후 검출)**: 생성된 본문을 검사해 위험 표현을 등급별로 표시.
   - 1차: 금지어 사전 기반 규칙 매칭(즉시, 무료)
   - 2차: LLM 판정(문맥 기반, 규칙으로 못 잡는 것)
3. **차단 단계**: **위험(빨강) 항목이 1개라도 남아 있으면 `복사하기` 버튼 비활성화.**
   - 각 지적사항에 **대안 문구 1개를 함께 제시**하고, 클릭 한 번으로 교체.
   - 교체 후 자동 재검사 → 전부 해소되면 복사 버튼 활성화.

### 6.2 등급
| 등급 | 기준 | 처리 |
|---|---|---|
| 🔴 위험 | 명백한 위반 소지 | 복사 차단. 교체 필수. |
| 🟡 주의 | 해석에 따라 문제될 수 있음 | 경고 표시, 복사는 허용 |
| 🔵 참고 | 표현 개선 권장 | 안내만 |

### 6.3 검출 대상 (초기 사전)

**금지 5요소(§5.6) 축으로 정리한다.** 사전의 각 항목은 자기가 어느 요소에 속하는지(`element: 1~5`)를 갖는다.
모드에 따라 같은 표현의 등급이 달라지기 때문이다.

| 요소 | 검출 대상 | 정보성 | 홍보성 |
|---|---|---|---|
| ① 효과 보장·최상급·비교우위 | `최고` `최상` `유일한` `1등` `국내 유일` `최초` / `100%` `완치` `부작용 없는` `무조건` `영구적` / 타 병원 지칭 비교, `~보다 우수한` | 🔴 | 🔴 |
| ② 가격·이벤트·할인 | 비급여 가격 표기 시 조건 미기재, 과도한 할인, `선착순 무료`, 이벤트성 금품 제공 | 🔴 | 🟡 |
| ③ 전후사진 | `전후사진`, `시술 전/후 비교` | 🔴 | 🔴 |
| ④ 체험담·후기 | 환자 1인칭 치료경험담 서술 | 🔴 | 🔴 |
| ⑤ 예약 유도 | 글 말미 예약·상담 유도 문구 | 🔴 | 🔵 |

**모드 무관 공통 검출**
- 지역명 + 시술명 조합 과다 반복 (검색 어뷰징 및 심의 리스크)
- 전문의·의료인 자격 오인 유발 표현

> 사전은 `config/compliance.json`으로 분리해 관리자가 추가할 수 있게 한다.
> **주의:** 이 기능은 보조 도구이며 법률 자문이 아니다. 최종 책임은 게시자에게 있음을 결과 화면 하단에 상시 고지한다.

---

## 7. 플랫폼별 출력 포맷

| 플랫폼 | 출력 형태 | 특이사항 |
|---|---|---|
| 네이버 블로그 | 서식 없는 텍스트, 문단 사이 빈 줄, 소제목은 굵기 대신 기호 처리 | 스마트에디터 붙여넣기 최적화. 해시태그 10개 별도 블록으로 제공. |
| 워드프레스·자사 홈페이지 | 마크다운 + HTML 토글 | H2/H3 구조 유지, 메타 디스크립션 별도 제공 |
| 인스타·스레드 | 캡션형 (짧게 재구성) | 본문에서 요약 추출 + 해시태그 |
| 티스토리·브런치 | 마크다운 | — |

복사 버튼은 선택된 플랫폼 포맷으로 복사하되, `다른 포맷으로 보기` 드롭다운을 제공한다.

---

## 8. 기술 구조

```
[GitHub Pages]                         [Render — 신규 서비스]
 index.html  ──── HTTPS/JSON ────────▶  FastAPI (Python)
 app.js                                  ├─ 인증 (공통 비밀번호)
 styles.css                              ├─ 키워드/제목/본문 생성
 config/*.json                           ├─ 의료법 검사
 (정적, 무료)                             ├─ 이력 저장/조회
                                         └─ Anthropic API 호출 (키는 서버 환경변수)
                                                    │
                                              [Claude API]
```

### 8.1 프론트엔드
- **바닐라 HTML/CSS/JS 단일 페이지** (빌드 도구 없음). 비개발자 유지보수와 GitHub Pages 배포에 유리.
- 프레임워크 미사용. 상태는 단순 JS 객체로 관리.
- 폰트: Pretendard (CDN)
- 아이콘: Lucide (CDN) 또는 인라인 SVG
- 파일 구조:
  ```
  /index.html
  /assets/styles.css
  /assets/app.js
  /config/branches.json
  /config/procedures.json
  /config/personas.json
  /config/audiences.json
  /config/tones.json
  ```

### 8.2 백엔드
- FastAPI + Uvicorn, Render 무료 또는 Starter 플랜.
- **API 키는 Render 환경변수에만 존재.** 프론트로 절대 내려보내지 않는다.
- CORS는 GitHub Pages 도메인만 허용.
- 무료 플랜 사용 시 콜드스타트(최대 50초) 발생 → 프론트에 "서버 깨우는 중" 안내 필요. **Starter 플랜 권장.**

### 8.3 폴백 모드 (서버 사용 불가 시)
- 백엔드 응답 실패가 반복되면 설정 화면에 **"개인 API 키로 사용하기"** 옵션 노출.
- 개인 키는 `localStorage`에만 저장, 서버로 전송하지 않음, 브라우저에서 Anthropic API 직접 호출.
- 이 모드에서는 이력 저장이 로컬 전용으로 동작.
- **기본은 항상 서버 모드.** 폴백은 숨겨진 보조 경로.

### 8.4 모델 선정 (비용 최적화)
| 단계 | 모델 | 사유 |
|---|---|---|
| 키워드 추천 | Claude Haiku | 짧은 출력, 빈도 높음 |
| 제목 추천 | Claude Haiku | 동일 |
| 본문 생성 | Claude Sonnet | 품질이 결과물의 전부 |
| 의료법 2차 검사 | Claude Haiku | 판정 위주 |
| 문단 재생성 | Claude Sonnet | 본문과 동일 품질 필요 |

---

## 9. API 명세 (초안)

모든 요청은 헤더 `Authorization: Bearer <session_token>` 필요.

| 메서드 | 경로 | 요청 | 응답 |
|---|---|---|---|
| POST | `/api/auth` | `{password}` | `{token, expires_at}` |
| GET | `/api/config` | — | 지점·시술·페르소나 등 전체 기준정보 |
| POST | `/api/keywords` | `{mode, lang, branch, procedure, persona, audience, tone, platform}` | `{keywords:[{text, intent, volume_hint}]}` |
| POST | `/api/titles` | `{...위 + selected_keywords[]}` | `{titles:[{text, length, hook_type}]}` |
| POST | `/api/draft` | `{...위 + title, length, cta, reference}` | `{sections:[{heading, body}], hashtags[], meta_description}` |
| POST | `/api/draft/section` | `{draft_id, section_index, instruction?}` | `{heading, body}` |
| POST | `/api/compliance` | `{text, mode, lang}` | `{issues:[{level, element, phrase, reason, suggestion, offset}]}` |
| GET | `/api/history` | `?branch=&procedure=&q=&page=` | `{items[], total}` |
| POST | `/api/history` | `{draft payload}` | `{id}` |
| GET | `/api/history/{id}` | — | 단건 상세 |

- **`mode`**: `information` | `promotion` (§5.6). 필수. 서버는 `information`이면 `cta`를 무시한다.
- **`lang`**: 기본값 `"ko"`. Phase 1은 `ko`만 처리하지만, **파라미터 자리는 지금 만들어 둔다**.
  나중에 언어를 추가할 때 API 모양과 이력 데이터를 갈아엎지 않기 위함이다 (§14 #10).
- 본문 생성은 **SSE 스트리밍** 권장(2,000자 생성은 20~40초 소요 — 무응답 화면은 이탈 원인).
- 스트리밍이 부담되면 최소한 **단계별 진행 표시**(구조 잡는 중 → 본문 쓰는 중 → 검사 중)를 넣는다.

---

## 10. 데이터

### 10.1 기준정보 (`config/*.json`)
버튼은 **하드코딩하지 않고 JSON을 읽어 자동 렌더링**한다. 지점 추가 = JSON 한 줄 추가.

```json
// config/branches.json
[
  { "id": "gangnam", "name": "강남점", "active": true, "note": "" }
]

// config/procedures.json
[
  {
    "category": "레이저",
    "items": [
      { "id": "barog-core", "name": "바로그 코어 레이저", "aliases": ["코어레이저"], "signature": true }
    ]
  }
]

// config/personas.json
[
  {
    "id": "manager",
    "name": "상담실장 시점",
    "description": "친근하게 안내하는 톤",
    "voice": "...",
    "emphasis": ["상담 과정", "비용·소요시간"],
    "avoid": ["효과 단정"],
    "warning": null
  }
]
```

> Phase 1.5로 **관리자 탭**(비밀번호 별도)을 붙여 JSON을 화면에서 수정·저장할 수 있게 확장한다.
> 그 전까지는 정남이 저장소에서 JSON을 수정하고 push하면 즉시 반영된다.

### 10.2 이력 저장소 — 브라우저 저장 (2026-08-19 확정)

**외부 저장소를 두지 않는다.** 이력은 각자 자기 브라우저(`localStorage`)에만 쌓인다.
서버는 이력을 보관하지 않으므로 `/api/history` 엔드포인트도 만들지 않는다.

이 선택의 한계는 숨기지 않고 화면에 알린다.

| 한계 | 화면에서 하는 일 |
|---|---|
| 다른 사람이 만든 글은 안 보인다 | 이력 탭 상단에 "이 컴퓨터에서 만든 글만 보여요" 안내 |
| 브라우저 기록을 지우면 사라진다 | 같은 안내 줄에 함께 표기 |
| 다른 컴퓨터에서는 안 보인다 | 같은 안내 줄에 함께 표기 |
| 저장 용량 한계 (약 5MB) | 100건을 넘으면 오래된 것부터 지우고, 지웠다는 사실을 알린다 |

> 나중에 팀 공유가 필요해지면 Supabase를 붙인다. 그때를 위해 이력 읽기·쓰기는
> 한 곳(`historyStore`)에 모아 두고, 화면은 그 함수만 부른다.

이력 레코드:
```
id, created_at, author_label, mode, lang, branch, procedure, persona, audience, tone,
platform, keywords[], title, body, compliance_status, char_count, model_used, cost_estimate
```

---

## 11. 디자인 가이드 — 클리닉 미니멀

| 항목 | 기준 |
|---|---|
| 배경 | `#FFFFFF` / 섹션 구분은 `#FAF7F5` |
| 텍스트 | 본문 `#1F1B18`, 보조 `#6B625C` |
| 포인트 컬러 | 브랜드 원색 **`#B09B8E`** 기준 팔레트 (아래 §11.1) |
| 상태 컬러 | 위험 `#C4453D` / 주의 `#B4761F` / 성공 `#4A7C59` |
| 폰트 | Pretendard. 본문 16px / 행간 1.7 |
| 모서리 | 12~16px 라운드 |
| 그림자 | 매우 약하게 (`0 1px 3px rgba(0,0,0,0.06)`) 또는 1px 보더로 대체 |
| 여백 | 넉넉하게. 요소 간 최소 16px, 섹션 간 40px |
| 애니메이션 | 150~200ms ease-out. 과한 모션 금지 |
| 최대 폭 | 콘텐츠 960px 중앙 정렬 |

### 11.1 브랜드 컬러 팔레트

브랜드 원색 `#B09B8E`는 **흰 배경 대비 2.65:1**이라 텍스트나 "흰 글씨 + 원색 배경" 버튼으로 쓰면 접근성 기준(4.5:1)에 미달한다.
원색은 **면·보더·선택 상태 표시**에 쓰고, 텍스트와 주요 버튼은 진한 변형을 쓴다.

| 토큰 | HEX | 흰 배경 대비 | 용도 |
|---|---|---|---|
| `--brand-50` | `#FAF7F5` | — | 페이지 섹션 배경 |
| `--brand-100` | `#F2EBE6` | — | 선택된 칩 배경, 카드 hover |
| `--brand-200` | `#E3D6CE` | — | 보더, 구분선 |
| `--brand-300` | `#CFBCB0` | — | 비활성 요소 |
| `--brand-400` | `#B09B8E` | 2.65:1 | **브랜드 원색** — 아이콘 면, 포커스 링, 선택 보더, 진행 바 |
| `--brand-500` | `#9A8375` | 3.57:1 | 큰 텍스트(18px+), UI 보더 |
| `--brand-600` | `#7A6355` | 5.61:1 | **주요 버튼 배경(흰 글씨)**, 링크, 강조 텍스트 |
| `--brand-700` | `#5C4A3F` | 8.35:1 | 버튼 hover/active, 제목 강조 |

**적용 규칙**
- 주요 버튼(`키워드 추천받기` 등): 배경 `--brand-600` + 흰 글씨, hover 시 `--brand-700`
- 보조 버튼: 배경 흰색 + 보더 `--brand-200` + 글씨 `--brand-700`
- 선택된 칩: 배경 `--brand-100` + 보더 `--brand-400` + 글씨 `--brand-700` (첨부한 참고 UI와 동일한 방식)
- 비선택 칩: 배경 흰색 + 보더 `#E5E0DC` + 글씨 `#4A423D`
- 포커스 링: `--brand-400` 2px, 오프셋 2px
- 원색 위에 흰 글씨를 올리는 조합은 **금지**

### 마이크로카피 원칙
- 에러는 원인 대신 **다음 행동**을 말한다. ✕ "500 Internal Server Error" → ○ "잠시 문제가 생겼어요. 다시 눌러볼까요?"
- 버튼은 동사로. `키워드 추천받기`, `제목 추천받기`, `본문 생성하기`, `복사하기`
- 대기 중에는 반드시 무슨 일이 일어나는지 문장으로 알려준다.
- 빈 화면(이력 없음)에도 안내 문구와 다음 행동 버튼을 둔다.

### 접근성
- 대비비 4.5:1 이상, 클릭 영역 최소 44×44px, 키보드 탭 이동 가능, 포커스 링 유지.

---

## 12. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 응답 시간 | 키워드/제목 5초 이내, 본문 45초 이내(스트리밍으로 체감 단축) |
| 동시 사용 | 5명 동시 사용 시 문제 없을 것 |
| 비용 | 글 1건당 목표 300원 이하. 관리자 화면에 월 누적 사용량 표시(Phase 1.5) |
| 에러 처리 | API 실패 시 자동 1회 재시도 → 실패하면 사용자에게 쉬운 말로 안내 + 입력값 보존 |
| 입력 보존 | 새로고침·뒤로가기에도 진행 중이던 입력이 날아가지 않게 세션 저장 |
| 브라우저 | 크롬 최신 기준. 반응형은 1280px 데스크톱 우선, 모바일은 조회 가능 수준 |
| 보안 | API 키 서버 보관, CORS 도메인 제한, 비밀번호는 해시 비교, 요청 rate limit |
| 로그 | 서버에 생성 요청 로그 남기되 개인정보·환자정보는 저장하지 않음 |

---

## 13. 완료 기준 (Phase 1)

- [ ] 링크 접속 → 비밀번호 입력 → 첫 글 완성까지 **비개발자가 도움 없이 3분 이내** 수행 가능
- [ ] 지점·시술·페르소나·독자·톤·플랫폼·분량을 모두 버튼으로 선택 가능
- [ ] 키워드 10개 이상 추천, 3~5개 선택 가능
- [ ] 제목 5개 추천, 선택 후 인라인 수정 가능
- [ ] 선택한 제목 기반 본문이 지정 분량 ±20% 내로 생성
- [ ] 소제목 블록 단위 재생성 동작
- [ ] **정보성 모드**에서 `이벤트·가격·CTA` 입력란이 보이지 않고, 요청에 `cta` 값을 강제로 넣어도 **서버가 무시**함
- [ ] **정보성 모드**에서 페르소나 목록에 `후기형`이 나타나지 않음
- [ ] 의료법 검사가 금지 표현을 검출하고 대안 문구를 제시하며, 위험 항목 잔존 시 복사 버튼이 비활성화됨
- [ ] 플랫폼별 포맷으로 복사 시 네이버 스마트에디터에 줄바꿈이 깨지지 않고 붙여넣어짐
- [ ] 팀 전체가 이력 목록을 조회·검색·재사용 가능
- [ ] 지점을 JSON에 1줄 추가하면 코드 수정 없이 버튼이 늘어남
- [ ] API 키가 브라우저 어디에서도 노출되지 않음 (개발자 도구 확인)

---

## 14. 결정 필요 / 미확정 항목

| # | 항목 | 선택지 | 권장 |
|---|---|---|---|
| ~~1~~ | ~~이력 저장소~~ | — | ✅ 확정 (2026-08-19) — **외부 저장소를 쓰지 않는다.** 이력은 각자 브라우저에만 저장한다(localStorage). 팀 공유가 필요 없다는 판단. 대신 그 한계를 화면에 분명히 알린다 |
| 2 | ~~Render 플랜~~ | — | ✅ 확정 (2026-08-14) — **무료 플랜으로 시작**. 콜드스타트는 접속 시 `/api/health` 선호출 + "서버 깨우는 중" 안내로 대응. 대기가 불편하면 대시보드에서 Starter($7)로 승격 |
| 3 | ~~저장소 공개 여부~~ | — | ✅ 확정 (2026-08-14) — **Public**. GitHub Pages 무료 사용. 비밀번호·API 키는 저장소에 두지 않고 Render 환경변수로만 관리 |
| ~~4~~ | ~~작성자 구분~~ | — | ✅ 확정 (2026-08-19) — **필요 없음.** 이력이 각자 브라우저에만 남으므로 누가 썼는지 구분할 대상이 없다 |
| 5 | ~~지점·시술 실제 목록~~ | — | ✅ 확정 — 지점 24개 / 시술 10개 카테고리 237개 (§16) |
| 6 | ~~브랜드 포인트 컬러~~ | — | ✅ 확정 — `#B09B8E` 기준 팔레트 (§11.1) |
| 7 | 기존 시스템의 지점별 스타일 학습 데이터 | 이식 여부 | 있으면 참고자료 프리셋으로 재활용 권장 |
| 8 | 시그니처 시술 `바로그 코어 레이저` | 첨부한 시술 목록에 없음 | 어느 카테고리에 넣을지 확정 필요 (`signature: true` 플래그로 목록 최상단 고정 예정) |
| 9 | `여드름·트러블` 의 `헤어컬러피부` | 항목명 확인 필요 | 카테고리와 맞지 않아 보임 — 오기/이동 여부 확인 |
| ~~10~~ | ~~콘텐츠 노선 (금지 5요소 처리)~~ | — | ✅ 확정 (2026-08-19) — **정보성 / 홍보성 2개 모드로 분리** (§5.6). 정보성에서는 위반 소재를 입력조차 받지 않고, 차단은 서버에서 강제 |
| ~~11~~ | ~~다국어 (영·일·중·태)~~ | — | ✅ 확정 (2026-08-19) — **Phase 1 이후로 미룸**. 근거: ①법무·심의 검토 결과 미도착 ②언어별 금지어 사전의 근거가 없음 ③중국어(샤오홍슈)는 별도 파이프라인 ④현지인 검수자 미계약. 단 `lang` 파라미터·이력 필드 자리는 **지금 확보**(§9, §10.2) |
| ~~12~~ | ~~팩트시트(의료진 인터뷰) 입력 방식~~ | — | ✅ 확정 (2026-08-19) — **현행 유지**. §5.2 `참고자료` 자유 텍스트 8,000자를 그대로 쓴다 |

**남은 운영 과제 (개발 범위 밖, 병행 진행)**

| 항목 | 왜 시급한가 |
|---|---|
| 의료광고 심의기관 질의 또는 의료법 전문 법무 검토 | 해외 사이트 구조와 언어별 금지어 사전의 **유일한 근거**. 답이 오는 데 시간이 걸리므로 개발과 병렬로 지금 착수. 이것 없이 쌓은 콘텐츠는 나중에 통째로 손봐야 할 수 있다 |
| 파일럿 카테고리 1개 선정 | 경쟁이 센 영역이 아니라, `바로그 코어 레이저`(`antiaging-032`)와 직결되면서 경쟁 밀도가 낮은 영역 |
| 원장님 격주 30분 인터뷰 일정 고정 | §5.2 `참고자료`에 넣을 팩트시트의 원천. 이게 없으면 결과물이 일반적인 AI 글과 구분되지 않는다 |

---

## 16. 기준정보 확정본

### 16.1 지점 (24개)
강남 · 서면 · 덕천 · 해운대 · 동성로 · 발산 · 강릉 · 하단 · 포항 · 구미 · 울산 · 양산 · 경주 · 거제 · 군산 · 익산 · 진해 · 유성 · 이천 · 아산 · 부천 · 진주 · 울산동구 · 평택

- `config/branches.json`으로 제공. 각 항목은 `{id, name, active, note}` 구조.
- **지점은 추가·삭제가 수시로 발생한다.** 삭제 시 항목을 지우지 말고 `active: false`로 바꾼다 — 과거 이력의 지점명이 깨지지 않게 하기 위함.
- 화면에는 `active: true`인 지점만 버튼으로 렌더링한다.

### 16.2 시술 (10개 카테고리 / 237개)

| 카테고리 | 개수 | ID prefix |
|---|---|---|
| 쁘띠·주사 | 48 | `petit-###` |
| 리프팅·탄력 | 26 | `lifting-###` |
| 홍조·혈관 | 11 | `vascular-###` |
| 기미·잡티·색소 | 32 | `pigment-###` |
| 흉터·모공·결 | 18 | `scar-###` |
| 여드름·트러블 | 14 | `acne-###` |
| 제모 | 18 | `hairremoval-###` |
| 스킨케어·관리 | 20 | `skincare-###` |
| 안티에이징·재생 | 31 | `antiaging-###` |
| 바디·체형 | 19 | `body-###` |

- `config/procedures.json`으로 제공. `{id, name, description, active, items:[{id, name, aliases, signature, active}]}` 구조.
- **선택 UI는 반드시 2단계**로 한다: 카테고리 칩 1줄 → 해당 카테고리 시술 칩. 237개를 한 번에 펼치면 화면이 무너진다. (첨부한 참고 UI와 동일한 방식)
- 시술 칩 영역 상단에 **검색 입력 한 칸**을 둔다. 48개짜리 카테고리는 눈으로 찾기 어렵다. 검색 시에는 카테고리를 넘어 전체에서 찾는다.
- **중복 등재 항목** (2개 카테고리에 동시 존재, 의도된 것으로 간주):
  `스킨부스터` `PDRN주사` `콜라겐주사` `바이리즌` `포텐자` `프락셀` `글루타치온주사` `백옥주사` `아쿠아필`
  → 검색 결과에는 `시술명 (카테고리)` 형태로 표시해 구분한다. 프롬프트에는 선택된 카테고리를 함께 전달해 글의 맥락을 잡는다.
- `aliases` 배열은 검색어 매칭용(예: `울쎄라` → `울쎄라피`). 초기값은 비어 있으며 운영하며 채운다.
- 삭제 시 지점과 동일하게 `active: false` 처리.

---

## 15. 개발 순서 제안

1. 백엔드 스켈레톤 + 인증 + `/api/config` (Render 배포까지 먼저 뚫기)
2. 프론트 정적 화면 + 기준정보 JSON 렌더링 (API 없이 화면부터 완성)
3. 키워드 → 제목 → 본문 3단 파이프라인 연결 **+ 글 목적(모드) 도입**(§5.6)
4. 의료법 검사 + 복사 차단
5. 플랫폼별 포맷 변환
6. 이력 저장·조회
7. 디자인 마감(여백·모션·빈 화면·에러 문구)
8. 마케터 1명 대상 시범 사용 → 피드백 반영 → 전체 오픈
